#+title: Demo webserver
#+subtitle: axum + diesel + PostgreSQL demo
#+author: Dietr1ch

A minimal but complete Rust web server built with [[https://axum.rs][axum]] and
[[https://diesel.rs][Diesel]], backed by PostgreSQL.  Intended as a reference
for project scaffolding: Nix dev shell, database migrations, structured logging,
and a clean REST API.

* Prerequisites

- [[https://nixos.org/download][Nix]] with flakes enabled

* Quick start

** 1. Enter the dev shell

#+begin_src fish
  nix develop
#+end_src

If you use ~direnv~, this also works automatically via the included ~.envrc~.

[[https://devenv.sh][devenv]] powers this shell — it provides the Rust toolchain, sets
~DATABASE_URL~ and ~LISTEN_SOCKET~, makes ~diesel~, ~cargo~, ~just~, and ~psql~
available, and can manage a PostgreSQL service.

** 2. Start PostgreSQL

#+begin_src fish
  devenv up
#+end_src

This starts PostgreSQL as a background service (along with any other configured
processes).  Press ~Ctrl+C~ to stop.  If you prefer to run PostgreSQL yourself,
set ~services.postgres.enable = false~ in ~devenv.nix~.

** 3. Create the database

#+begin_src fish
  just setup
#+end_src

This runs ~diesel setup~ (creates the database if missing) then applies all
pending migrations.

** 4. Start the server

#+begin_src fish
  just run
#+end_src

The server listens on ~$LISTEN_SOCKET:="0.0.0.0:3000"~.

** 4. Try it out

Open this file in Emacs with ~verb-mode~ enabled, then place point on any
heading under the [[*API reference][API reference]] section below and press ~C-c C-r C-r~
to send the request.

* API reference

All endpoints return JSON.  Errors follow the shape ~{"error": "message"}~.

The headings below are live requests — open this file in Emacs with ~verb-mode~
and press ~, r r~ (~C-c C-r C-r~) while on any of them to send the request.

** demo-server                                                        :verb:
template http://localhost:3000

*** Health check
get /health

*** List users
get /users

*** Get user
get /users/<uuid>

*** Create user
post /users
Content-Type: application/json

{"name": "Alice", "email": "alice@example.com"}

* Configuration

The application reads these environment variables:

| Variable        | Default                            | Description                  |
|-----------------+------------------------------------+------------------------------|
| ~DATABASE_URL~  | ~postgres://localhost:5432/demo~   | PostgreSQL connection string |
| ~LISTEN_SOCKET~ | ~0.0.0.0:3000~                     | Socket address to listen on  |
| ~RUST_LOG~      | ~demo_server=info,tower_http=info~ | Tracing/logging filter       |

Variables can be set in a ~.env~ file (loaded automatically on startup).

* Development

** Project layout

- ~src/bin/server.rs~ — entry point, logging, connection pool, auto-migrations
- ~src/lib.rs~ — crate root, re-exports modules, defines shared types (~DbPool~)
- ~src/error.rs~ — unified ~AppError~ type and ~IntoResponse~ impl
- ~src/routes/mod.rs~ — axum router; delegates to per-resource handler modules
  - ~src/routes/health.rs~ — health-check endpoint
  - ~src/routes/user.rs~ — user CRUD handlers
- ~src/models/~
  - ~src/models/mod.rs~ — module root
  - ~src/models/user.rs~ — Diesel model structs (~User~, ~NewUser~)
- ~src/schema.rs~ — Diesel table! macro (auto-generated by ~diesel print-schema~)
- ~migrations/~ — SQL migration files
- ~devenv.nix~ / ~devenv.yaml~ — [[https://devenv.sh][devenv]] configuration (Rust, PostgreSQL, packages)
- ~flake.nix~ — Nix flake entry point, delegates to ~devenv~
- ~Justfile~ — common task runner commands

** Common commands

| Command                 | Description                                   |
|-------------------------+-----------------------------------------------|
| ~devenv up~             | Start background services (DB)                |
| ~just run~              | Start the development server                  |
| ~just check~            | Run ~cargo check~                             |
| ~just test~             | Run ~cargo test~                              |
| ~just lint~             | Run ~cargo clippy -- -D warnings~             |
| ~just fmt~              | Format code with ~rustfmt~                    |
| ~just migrate~          | Run pending Diesel migrations                 |
| ~just migration <name>~ | Create a new migration scaffold               |
| ~just db-reset~         | Drop and recreate the database                |
| ~just db-clean-state~   | Nuke devenv's pg state (after version switch) |
| ~just ci~               | Full CI pipeline: check + test + lint         |

Run ~just~ (no arguments) to see the full list.

* License

This project is distributed under the terms of the MIT license.
See the LICENSE file (if present) for details.


* Meta                                                              :ARCHIVE:
# Local Variables:
# eval: (verb-mode)
# End:
